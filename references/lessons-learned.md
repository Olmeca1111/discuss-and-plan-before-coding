# Lessons learned

Transferable development methods captured while using this skill. This file is
**not** loaded automatically — it is read at the start of a project's planning and
consulted when a relevant situation arises. Keeping lessons here (rather than in
SKILL.md) means they cost nothing until something actually opens this file.

## Rules for this file
- Only **transferable methods** belong here — things that would change how you'd
  approach a future, *unrelated* project. Project-specific quirks go in that
  project's `CLAUDE.md`, not here. (See the "methodology filter" in SKILL.md.)
- Nothing is added without an explicit user "yes" to a `🔧 SKILL IMPROVEMENT PROPOSAL`.
- On adding a lesson that already exists, don't duplicate — add the project to its
  "Seen on" list and increment the count.
- A lesson promoted into SKILL.md (after 3+ distinct projects) stays here marked
  `Status: promoted`, so the history is preserved.
- Prune at the Phase 7 retrospective: collapse duplicates, drop the obsolete, keep
  entries terse. Log non-trivial prunes in `CHANGELOG.md`.

## Lesson format
```
### <short lesson title>
- Pattern: <the method>
- Seen on: <project-a>, <project-b>   (count: 2)
- Status: lesson | promoted
```

## Lessons
<!-- No lessons captured yet. The first approved proposal goes below this line. -->
